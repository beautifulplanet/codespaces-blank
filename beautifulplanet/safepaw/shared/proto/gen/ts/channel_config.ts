// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v5.29.5
// source: channel_config.proto

/* eslint-disable */
import { Timestamp } from "./common";

export const protobufPackage = "nopenclaw.channel";

/** Channel status */
export enum ChannelStatus {
  CHANNEL_STATUS_UNSPECIFIED = 0,
  /** ACTIVE - Channel is live and accepting messages */
  ACTIVE = 1,
  /** PAUSED - Temporarily stopped (maintenance) */
  PAUSED = 2,
  /** DISABLED - Permanently off */
  DISABLED = 3,
  /** ERROR - Channel is in an error state */
  ERROR = 4,
  UNRECOGNIZED = -1,
}

export function channelStatusFromJSON(object: any): ChannelStatus {
  switch (object) {
    case 0:
    case "CHANNEL_STATUS_UNSPECIFIED":
      return ChannelStatus.CHANNEL_STATUS_UNSPECIFIED;
    case 1:
    case "ACTIVE":
      return ChannelStatus.ACTIVE;
    case 2:
    case "PAUSED":
      return ChannelStatus.PAUSED;
    case 3:
    case "DISABLED":
      return ChannelStatus.DISABLED;
    case 4:
    case "ERROR":
      return ChannelStatus.ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ChannelStatus.UNRECOGNIZED;
  }
}

export function channelStatusToJSON(object: ChannelStatus): string {
  switch (object) {
    case ChannelStatus.CHANNEL_STATUS_UNSPECIFIED:
      return "CHANNEL_STATUS_UNSPECIFIED";
    case ChannelStatus.ACTIVE:
      return "ACTIVE";
    case ChannelStatus.PAUSED:
      return "PAUSED";
    case ChannelStatus.DISABLED:
      return "DISABLED";
    case ChannelStatus.ERROR:
      return "ERROR";
    case ChannelStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Rate limiting configuration */
export interface RateLimit {
  /** 0 = unlimited */
  maxMessagesPerMinute: number;
  /** 0 = unlimited */
  maxMessagesPerHour: number;
  /** Max burst before throttle */
  burstLimit: number;
}

/** Channel configuration */
export interface ChannelConfig {
  /** UUID v4 */
  id: string;
  /** Channel name (e.g., "discord") */
  channel: string;
  /** Which agent handles this channel */
  agentId: string;
  status: ChannelStatus;
  /**
   * Connection credentials (encrypted at rest)
   * SECURITY: These are stored encrypted in Postgres, never in plaintext
   */
  credentials: { [key: string]: string };
  /** Behavior settings */
  rateLimit:
    | RateLimit
    | undefined;
  /** Can users send images/files? */
  allowMedia: boolean;
  /** Does this channel support threading? */
  allowThreads: boolean;
  /** Platform-specific limit (e.g., 2000 for Discord) */
  maxMessageLength: number;
  /** Feature flags */
  features: { [key: string]: boolean };
  /** Metadata */
  createdAt: Timestamp | undefined;
  updatedAt: Timestamp | undefined;
}

export interface ChannelConfig_CredentialsEntry {
  key: string;
  value: string;
}

export interface ChannelConfig_FeaturesEntry {
  key: string;
  value: boolean;
}

/** List of all configured channels */
export interface ChannelRegistry {
  channels: ChannelConfig[];
}

function createBaseRateLimit(): RateLimit {
  return { maxMessagesPerMinute: 0, maxMessagesPerHour: 0, burstLimit: 0 };
}

export const RateLimit: MessageFns<RateLimit> = {
  fromJSON(object: any): RateLimit {
    return {
      maxMessagesPerMinute: isSet(object.maxMessagesPerMinute)
        ? globalThis.Number(object.maxMessagesPerMinute)
        : isSet(object.max_messages_per_minute)
        ? globalThis.Number(object.max_messages_per_minute)
        : 0,
      maxMessagesPerHour: isSet(object.maxMessagesPerHour)
        ? globalThis.Number(object.maxMessagesPerHour)
        : isSet(object.max_messages_per_hour)
        ? globalThis.Number(object.max_messages_per_hour)
        : 0,
      burstLimit: isSet(object.burstLimit)
        ? globalThis.Number(object.burstLimit)
        : isSet(object.burst_limit)
        ? globalThis.Number(object.burst_limit)
        : 0,
    };
  },

  toJSON(message: RateLimit): unknown {
    const obj: any = {};
    if (message.maxMessagesPerMinute !== 0) {
      obj.maxMessagesPerMinute = Math.round(message.maxMessagesPerMinute);
    }
    if (message.maxMessagesPerHour !== 0) {
      obj.maxMessagesPerHour = Math.round(message.maxMessagesPerHour);
    }
    if (message.burstLimit !== 0) {
      obj.burstLimit = Math.round(message.burstLimit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimit>, I>>(base?: I): RateLimit {
    return RateLimit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimit>, I>>(object: I): RateLimit {
    const message = createBaseRateLimit();
    message.maxMessagesPerMinute = object.maxMessagesPerMinute ?? 0;
    message.maxMessagesPerHour = object.maxMessagesPerHour ?? 0;
    message.burstLimit = object.burstLimit ?? 0;
    return message;
  },
};

function createBaseChannelConfig(): ChannelConfig {
  return {
    id: "",
    channel: "",
    agentId: "",
    status: 0,
    credentials: {},
    rateLimit: undefined,
    allowMedia: false,
    allowThreads: false,
    maxMessageLength: 0,
    features: {},
    createdAt: undefined,
    updatedAt: undefined,
  };
}

export const ChannelConfig: MessageFns<ChannelConfig> = {
  fromJSON(object: any): ChannelConfig {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      status: isSet(object.status) ? channelStatusFromJSON(object.status) : 0,
      credentials: isObject(object.credentials)
        ? (globalThis.Object.entries(object.credentials) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      rateLimit: isSet(object.rateLimit)
        ? RateLimit.fromJSON(object.rateLimit)
        : isSet(object.rate_limit)
        ? RateLimit.fromJSON(object.rate_limit)
        : undefined,
      allowMedia: isSet(object.allowMedia)
        ? globalThis.Boolean(object.allowMedia)
        : isSet(object.allow_media)
        ? globalThis.Boolean(object.allow_media)
        : false,
      allowThreads: isSet(object.allowThreads)
        ? globalThis.Boolean(object.allowThreads)
        : isSet(object.allow_threads)
        ? globalThis.Boolean(object.allow_threads)
        : false,
      maxMessageLength: isSet(object.maxMessageLength)
        ? globalThis.Number(object.maxMessageLength)
        : isSet(object.max_message_length)
        ? globalThis.Number(object.max_message_length)
        : 0,
      features: isObject(object.features)
        ? (globalThis.Object.entries(object.features) as [string, any][]).reduce(
          (acc: { [key: string]: boolean }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Boolean(value);
            return acc;
          },
          {},
        )
        : {},
      createdAt: isSet(object.createdAt)
        ? Timestamp.fromJSON(object.createdAt)
        : isSet(object.created_at)
        ? Timestamp.fromJSON(object.created_at)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? Timestamp.fromJSON(object.updatedAt)
        : isSet(object.updated_at)
        ? Timestamp.fromJSON(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: ChannelConfig): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.status !== 0) {
      obj.status = channelStatusToJSON(message.status);
    }
    if (message.credentials) {
      const entries = globalThis.Object.entries(message.credentials) as [string, string][];
      if (entries.length > 0) {
        obj.credentials = {};
        entries.forEach(([k, v]) => {
          obj.credentials[k] = v;
        });
      }
    }
    if (message.rateLimit !== undefined) {
      obj.rateLimit = RateLimit.toJSON(message.rateLimit);
    }
    if (message.allowMedia !== false) {
      obj.allowMedia = message.allowMedia;
    }
    if (message.allowThreads !== false) {
      obj.allowThreads = message.allowThreads;
    }
    if (message.maxMessageLength !== 0) {
      obj.maxMessageLength = Math.round(message.maxMessageLength);
    }
    if (message.features) {
      const entries = globalThis.Object.entries(message.features) as [string, boolean][];
      if (entries.length > 0) {
        obj.features = {};
        entries.forEach(([k, v]) => {
          obj.features[k] = v;
        });
      }
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = Timestamp.toJSON(message.createdAt);
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = Timestamp.toJSON(message.updatedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelConfig>, I>>(base?: I): ChannelConfig {
    return ChannelConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelConfig>, I>>(object: I): ChannelConfig {
    const message = createBaseChannelConfig();
    message.id = object.id ?? "";
    message.channel = object.channel ?? "";
    message.agentId = object.agentId ?? "";
    message.status = object.status ?? 0;
    message.credentials = (globalThis.Object.entries(object.credentials ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.rateLimit = (object.rateLimit !== undefined && object.rateLimit !== null)
      ? RateLimit.fromPartial(object.rateLimit)
      : undefined;
    message.allowMedia = object.allowMedia ?? false;
    message.allowThreads = object.allowThreads ?? false;
    message.maxMessageLength = object.maxMessageLength ?? 0;
    message.features = (globalThis.Object.entries(object.features ?? {}) as [string, boolean][]).reduce(
      (acc: { [key: string]: boolean }, [key, value]: [string, boolean]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Boolean(value);
        }
        return acc;
      },
      {},
    );
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null)
      ? Timestamp.fromPartial(object.createdAt)
      : undefined;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null)
      ? Timestamp.fromPartial(object.updatedAt)
      : undefined;
    return message;
  },
};

function createBaseChannelConfig_CredentialsEntry(): ChannelConfig_CredentialsEntry {
  return { key: "", value: "" };
}

export const ChannelConfig_CredentialsEntry: MessageFns<ChannelConfig_CredentialsEntry> = {
  fromJSON(object: any): ChannelConfig_CredentialsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ChannelConfig_CredentialsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelConfig_CredentialsEntry>, I>>(base?: I): ChannelConfig_CredentialsEntry {
    return ChannelConfig_CredentialsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelConfig_CredentialsEntry>, I>>(
    object: I,
  ): ChannelConfig_CredentialsEntry {
    const message = createBaseChannelConfig_CredentialsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseChannelConfig_FeaturesEntry(): ChannelConfig_FeaturesEntry {
  return { key: "", value: false };
}

export const ChannelConfig_FeaturesEntry: MessageFns<ChannelConfig_FeaturesEntry> = {
  fromJSON(object: any): ChannelConfig_FeaturesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Boolean(object.value) : false,
    };
  },

  toJSON(message: ChannelConfig_FeaturesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== false) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelConfig_FeaturesEntry>, I>>(base?: I): ChannelConfig_FeaturesEntry {
    return ChannelConfig_FeaturesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelConfig_FeaturesEntry>, I>>(object: I): ChannelConfig_FeaturesEntry {
    const message = createBaseChannelConfig_FeaturesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? false;
    return message;
  },
};

function createBaseChannelRegistry(): ChannelRegistry {
  return { channels: [] };
}

export const ChannelRegistry: MessageFns<ChannelRegistry> = {
  fromJSON(object: any): ChannelRegistry {
    return {
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => ChannelConfig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ChannelRegistry): unknown {
    const obj: any = {};
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => ChannelConfig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelRegistry>, I>>(base?: I): ChannelRegistry {
    return ChannelRegistry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelRegistry>, I>>(object: I): ChannelRegistry {
    const message = createBaseChannelRegistry();
    message.channels = object.channels?.map((e) => ChannelConfig.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
